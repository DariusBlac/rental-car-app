export const isLoadingSelector = state => state.catalog.isLoading;
export const allCarsSelector = state => state.catalog.carsAll;
export const carsSelector = state => state.catalog.cars;
export const brandsSelector = state => state.catalog.brands;
